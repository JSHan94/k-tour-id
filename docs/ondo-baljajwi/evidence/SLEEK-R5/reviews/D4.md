# SLEEK R5 — D4 truth, provenance, and privacy review

## Frozen tuple and blind declaration

| Field | Reviewed value |
|---|---|
| Candidate | `39687c33ca5d14b60304b762b373e719bb4bdbbd` |
| Product boundary | `9ec3d192d0ebdc9614d980bdb173633aee16fc17` |
| Harness boundary | `12354bcf71621c00a08433faf09cbb000683ae61` |
| Frozen baseline digest | `4cfbed3b2f2fba3d7813e0c3ff6a160bfcec9605c947d84d1bc35f73e97c314b` |
| Frozen target | `http://127.0.0.1:3219/ondo-b` |
| Reviewer lane | `D4 — truth / provenance / privacy` |

This was a strict-blind review of the tuple above. I did not inspect Git history, diffs, commit subjects, prior-review conclusions, issue/fix ledgers, other reviewers' work, old B, or `/ondo`. The only repository evidence read was the permitted R4 frozen receipt/inventory, B pixel-evidence and QA/visual helpers, the three B snapshot families, the three governing truth/design documents, and execution documents 01–07.

The in-app Browser was attempted first with an exact `getForUrl` lookup and the full runtime documentation. No browser was attached. After completing the Browser bootstrap-troubleshooting path and confirming the browser list was empty, I used the expressly permitted standalone Playwright Chromium fallback. The fallback loaded the actual frozen URL; it did not substitute source inspection for rendered-UI review.

## Verdict

**CLEAN**

Raw severity count: `S0 0 · S1 0 · S2 0 · S3 0`.

The decision rule was applied directly: any raw S0, S1, or S2 would make the lane `NOT CLEAN`. None was observed. No quorum or expected decision from another reviewer was considered.

## Actual rendered evidence

- `18/18` canonical flows ran at mobile and desktop projects: `36/36 PASS`.
- `126/126` checkpoint dispositions were accounted for: `121` actual rendered/functional dispositions and `5` reasoned `N/A` dispositions from the frozen contract.
- `46/46` cases and `44/44` distinct states were inspected in both the functional journeys and/or their rendered frozen visual states.
- `276/276` PNG comparisons passed without baseline update: `46/46` at each of `360×800`, `390×844`, `430×932`, `768×1024`, `801×1000`, and `1440×1000`.
- KO and EN claim surfaces were present throughout the case inventory. Claim-bearing truth/privacy exemplars were additionally read directly from the live DOM and visually inspected at representative mobile, threshold, tablet, and desktop widths.
- A focused profile probe passed opt-in, navigation return, reload persistence, explicit withdrawal, and post-withdrawal reload (`1/1 PASS`).

The exhaustive flow, checkpoint, case, state, locale, and viewport ledger is in `../coverage/D4.md`.

## Truth and provenance assessment

| Claim class | Rendered evidence | D4 assessment |
|---|---|---|
| Sourced place identity | City rows say `Official Korean source name`; place detail identifies `MOIS LOCALDATA`, an active general-restaurant licence record, snapshot date, and source-record ID. | Source and record status are claim-near; no provider laundering. |
| Generated text | English transliterations say `Generated, not an official English name`. | Generated labels are not presented as official-source values. |
| Simulated ranking | Scores are labelled `Simulated score`, fixed snapshot, `Not live`, and not activity/trends. | The visual hierarchy does not turn a mock score into a live popularity claim. |
| Unknown place facts | Hours, alcohol, admission, and age-restriction fields render `Not confirmed by this source`; After19 says it is ONDO policy, not an official restriction. | Unknowns remain unknown; absence is not converted into a fact. |
| Stale/limited evidence | Labs merchant-trait failure renders `Out of date`; retry yields only a specific access condition and explicitly does not guarantee admission. | Staleness and capability limits are visible at the decision point. |
| Tables and hosts | Tables say fixed/simulated preview, not current availability, no live host/reservation, and local-preview join only; host attributes are `self-declared`; no nationality/gender matching claim is made. | Social availability and host identity are not overstated. |
| Identity providers | Passport is provider-neutral; OmniOne CX is a simulation; Residence Card is visibly `NOT CONNECTED`/unconfigured with an alternate. The flow says preview only and no request is sent externally. | Provider choice, configured capability, and real network activity remain distinct. |
| Credential axes | Account, identity check, 19+, and Payment KYC are separate. Completing one changes only its own state. | No credential bundling or implied capability escalation. |
| Payment | Checkout says simulated, no real payment/assets, illustrative total, OOKRW test token with no redemption/1:1 promise, and read-only settlement hypothesis. | No real-payment, monetary-value, or settlement claim. |
| Reputation | Identity, simulated visits, local-preview contributions, and local-preview Tables remain four axes and explicitly are not proof of safety, character, expertise, or real-world activity. | No synthetic universal trust/safety score. |
| Labs | Labs is experimental/simulated, uses no real assets or production service, and bridge/receipt outcomes are read-only simulations. | Experimental capability is never represented as production execution. |

## Privacy and consent assessment

| Lifecycle point | Reproduction | Expected | Actual rendered result |
|---|---|---|---|
| Entry | Fresh account session → `ID` → profile panel. | Private/default-local state before consent. | `Browser-local profile preview` and `Private in this browser session by default`; identity-check nationality is never copied. |
| Edit modal | `Edit profile preview` → include `From`. | Granular, reversible, per-field consent. | Each field has an explicit included/excluded state; location/language are labelled self-declared. |
| Cancel | Toggle a field → `Cancel preview editing`. | Prior consent state remains unchanged. | Cancel preserved the prior private selection. |
| Failure/retry | `?profile=failure` → save → retry. | Failure must not silently publish or lose the prior state. | Visible alert; prior state retained; `Try saving preview again` reaches only the selected browser preview. |
| Terminal | Save one selected field. | Only the selected field is included; nothing external is implied. | `Selected fields included in this browser preview`; surrounding copy says nothing is published or sent. |
| Return | Leave ID for ONDO, then return to ID. | Browser-session selection remains coherent. | The selected field remained included on return. |
| Reload | Reload after opt-in. | Session-local selection persists only as described. | Selection persisted within the same browser session; no external/public claim appeared. |
| Withdraw | Re-enter edit, remove the included field, save. | Consent can be withdrawn without an external side effect. | Panel returned to the private/default-local presentation. |
| Reload after withdrawal | Reload, then return to ID. | Withdrawn field stays withdrawn. | Private/default-local state remained; selected-field inclusion text stayed absent. |
| Local signal | Submit note/photo success and failure routes. | Content remains local in this simulation; failure preserves the draft. | UI says simulated contribution, saved to this-device demo, nothing sent live; photo is not uploaded or server-stored. |
| Chat image | Joined Table → add photo → deterministic failure/retry. | No hidden upload or verified-participant claim. | Messages/photos are session simulation; image is not uploaded/stored; participant identities are not verified. |
| Local reputation | Complete local signal/feedback/Table activities. | Local activity must not mutate unrelated identity, age, payment, or safety claims. | Only visit/contribution/Table preview axes changed; identity/age/payment/stamps invariants remained separate. |

## Raw findings register

No raw finding met S0, S1, S2, or S3. Therefore there is no finding fingerprint, remediation target, or unresolved repro to record.

For avoidance of doubt, the review actively tested the failure/cancel/retry paths rather than treating happy-path disclaimer copy as sufficient. Provider failures remained visibly simulated or unsupported; checkout failure created no receipt/stamp; profile save failure retained prior consent; local-signal and chat-image failures retained local-only boundaries; stale Labs evidence did not become an admission or safety guarantee.

## Final D4 disposition

The frozen candidate preserves provenance at the claim, keeps sourced/generated/simulated/unknown/stale/browser-local states semantically distinct, does not overstate provider/payment/reputation capabilities, and supplies granular reversible browser-session consent. D4 is **CLEAN** for this exact tuple only.
