# SLEEK R5 retry — D2 coverage

Reviewer lane: interaction, task continuity, keyboard/focus, context, responsive KO/EN, rapid input, and filtered-map truth.

Verdict linkage: [`reviews/D2.md`](../reviews/D2.md) — **NOT CLEAN · S0 0 / S1 0 / S2 1**.

## Frozen tuple and registry census

| Check | D2 result |
|---|---|
| Candidate | `b0d25fe634d9d50a8668501f0fde5f641153168b` |
| Product / Harness | `30dcb136c697e3f57d8e3beab6ee31ea37bd1acc` / `ee19adb2a5fce5bea7e0aeb6a8caac80ca65bd2f` |
| Flow registry | `18` flows; `126` checkpoints; `121 ACTUAL`; `5` reasoned N/A; `0 GAP` |
| Visual registry | `47` cases; `45` distinct state IDs; locale mix `31 EN / 16 KO` |
| Frozen viewports | `360×800`, `390×844`, `430×932`, `768×1024`, `801×1000`, `1440×1000` |
| PNG inventory | `282` unique paths and `282` unique `case + viewport` pairs; `47` at every viewport |
| PNG dimensions | `0/282` actual PNG-to-TSV mismatches |
| Provenance | `18 R5-FIX + 264 R5-CARRY`; all `282` rows FROZEN |
| TSV | SHA-256 `59c2855ca53b66186a02b94978b15811d5ddedc1aaf73178816db032b7fca576` |
| Baseline digest | exact `f1ec9b0c6a3f10f77495bda996a4eb09a30f30743eb037f4c65ad39c3c1dfe91` |

Counts were independently recovered from the allowed canonical helpers and [`baseline-files.tsv`](../../SLEEK-R5/baseline-files.tsv), then reconciled to the [`frozen-receipt.md`](../../SLEEK-R5/frozen-receipt.md) and [pixel evidence registry](../../../../../k-tour-id-app/tests/evidence/ONDO_B_PIXEL_EVIDENCE.md). No prior reviewer file or issue/fix ledger was used.

## Direct Chromium matrix

| Area | Evidence exercised | Result |
|---|---|---|
| Six widths × KO/EN | Nation and Seoul City at all `12` locale/viewport combinations (`24` rendered state observations) | PASS: no horizontal overflow, unnamed visible control, sub-44px visible control, nav overlap, or page error |
| Short landscape | Nation at `844×390` in EN and KO | PASS: cards, title, metadata, and nav remain separated |
| Task flow/context | Nation → Seoul → List → Roba peek → detail → guest Save → Account Gate | PASS through gate; Escape returns to the same detail and Save focus |
| Keyboard/focus | After19 prompt focus loop, inert background, focus token, Escape, focus return; place detail/gate Escape | PASS |
| Rapid input | Double After19 toggle; double Save activation; nine immediate filtered List/Map toggles | PASS: at most one modal/canvas; stable terminal state |
| Filtered map | Exact one-result Korean-name query at `390×844` and `1440×1000` | PASS: list `1`, source `1`, rendered `1`, marker/key score `59`, truthful filtered legend |
| Browser Back/Forward | Open Roba detail at `390×844` and `1440×1000`, then Back and Forward | **FAIL / S2**: Back leaves the task; Forward remounts Nation and does not restore city/place context |
| Frozen focus snapshots | `PLACE-DETAIL` and `AFTER19-VENUE-LOCKED` R5-FIX frames at mobile/desktop | PASS: focus uses the distinct blue interaction token, not the danger semantic color |
| Frozen filtered snapshots | `CITY-FILTERED-MAP` at the six committed widths | PASS: one marker and one-result legend are mutually consistent |

## Coverage boundaries

- The `18/126` registry was audited structurally and reconciled to the frozen automated receipt; D2 did not rerun the sealed full nonpixel suite.
- The `47/45/282/6` pixel contract was checked by census, uniqueness, actual dimensions, provenance, exact digest, targeted snapshot inspection, and direct responsive interaction; D2 did not update snapshots.
- KO and EN were both exercised directly. The complete registry’s frozen distribution is `186 EN + 96 KO = 282` PNGs.
- Runtime/browser evidence came only from the supplied local URL and actual Chromium fallback. Temporary review screenshots were kept outside the repository and are not release artifacts.

D2 coverage is complete for the assigned lane. The sole release blocker is `R5R-D2-001` in the linked review.
